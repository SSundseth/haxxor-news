<% @stories.chrono.each do |story| %>

  <div class="story">
    <%= link_to story.title, story.url %>    
    <div class="info">
      <%= story.score %> points |
      <%= link_to story.comment_count.to_s + " comments", story_path(story) %> |
      Submitted <%= pretty_time story.created_at %> by <%= story.user.email %>
    </div>      
  </div>
<% end %>

<%= will_paginate @stories %>
